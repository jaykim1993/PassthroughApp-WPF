<Window x:Class="PassthroughApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PassthroughApp.ViewModels"
        Title="🎙 Audio Passthrough"
        Width="400" Height="300"
        ResizeMode="NoResize"
        Background="#111111"
        WindowStartupLocation="CenterScreen">

    <StackPanel Margin="32">
        <StackPanel.Resources>
            <!-- CornerRadius를 위한 버튼 스타일 -->
            <Style TargetType="Button">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.85" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Opacity" Value="0.7" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </StackPanel.Resources>

        <!-- Status text -->
        <TextBlock Text="{Binding StatusText}"
                   Foreground="#888888"
                   FontSize="12"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,16" />

        <!-- Latency display -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,16">
            <TextBlock Text="Latency"
                       Foreground="#555555"
                       FontSize="12"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0" />
            <TextBlock Text="{Binding LatencyText}"
                       Foreground="#00C878"
                       FontSize="28"
                       FontWeight="Bold" />
        </StackPanel>

        <!-- Toggle button -->
        <Button Content="{Binding ButtonLabel}"
                Command="{Binding ToggleCommand}"
                HorizontalAlignment="Center"
                Width="160" Height="48"
                FontSize="15"
                FontWeight="SemiBold"
                Background="#2A2A2A"
                Foreground="White"
                BorderThickness="0"
                Cursor="Hand" />

    </StackPanel>
</Window>
